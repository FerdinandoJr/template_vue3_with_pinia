export enum ServiceStatus {
    IN_PROGRESS = 'in_progress',
    WAITING = 'waiting',
    PAUSED = 'paused',
    FINISHED = 'finished',
    CANCELED = 'canceled'
}

export enum ServicePriority {
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high',
    URGENT = 'urgent'
}